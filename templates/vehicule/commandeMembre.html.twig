{% extends "base.html.twig" %}

{% block title %}Recapitulatif de votre commande{% endblock %}

{% block body %}




<div class="container card my-5 p-4">
    <h1 class="text-center my-3">Recapitulatif de votre commande</h1>  
    <div class="row p-5">
        <div class="col-6 d-flex align-center justify-content-center">
        <img src="{{ commande.vehicule.photo }}" alt="">
        </div>
        <div class="card-body col-6">
        <h4 class="card-title">{{ commande.vehicule.titre }}</h4>
        
        <p class="card-text md-3">{{ commande.vehicule.description }}</p>
        <div class="row">
            
        </div>
        <h5 class="card-subtitle mt-4 text-muted">Date de départ : </h5>
        <p class="mt-3">{{ commande.DateHeureDepart|date('d/m/Y H:i') }}</p>
        

        </div>
    </div>
  
</div>
 

{% endblock %}